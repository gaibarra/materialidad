# ═══════════════════════════════════════════════════════════════════════
#  Materialidad Backend – Variables de entorno
#  Copia este archivo como .env y asigna valores reales.
#  cp .env.template .env
# ═══════════════════════════════════════════════════════════════════════

# ── Django core ───────────────────────────────────────────────────────
# Genera con: python -c "from django.core.management.utils import get_random_secret_key; print(get_random_secret_key())"
DJANGO_SECRET_KEY=cambia-esto-por-una-clave-segura

# False en producción (default)
DJANGO_DEBUG=False

# Hosts separados por coma
DJANGO_ALLOWED_HOSTS=materialidad.online,www.materialidad.online

# Orígenes confiables para CSRF (incluir esquema https://)
DJANGO_CSRF_TRUSTED_ORIGINS=https://materialidad.online,https://www.materialidad.online

# ── Base de datos (PostgreSQL DSN) ───────────────────────────────────
# Formato: postgres://USUARIO:CONTRASEÑA@HOST:PUERTO/BASE_DE_DATOS
DJANGO_CONTROL_DB_URL=postgres://materialidad:contraseña_segura@localhost:5432/materialidad_control

# ── CORS ──────────────────────────────────────────────────────────────
CORS_ALLOW_ALL_ORIGINS=False
CORS_ALLOWED_ORIGINS=https://materialidad.online,https://www.materialidad.online

# ── JWT ───────────────────────────────────────────────────────────────
# Formato ISO 8601 duración o HH:MM:SS
JWT_ACCESS_TTL=0:30:00
JWT_REFRESH_TTL=1 00:00:00

# ── Tenancy ───────────────────────────────────────────────────────────
TENANT_REQUIRED_PATHS=/api/materialidad/
TENANT_FREE_LIMIT=1

# ── n8n (opcional) ────────────────────────────────────────────────────
# N8N_WEBHOOK_URL=https://n8n.ejemplo.com/webhook/xxx
# N8N_API_KEY=
# N8N_TIMEOUT_SECONDS=30

# ── AI Providers ──────────────────────────────────────────────────────
AI_PROVIDER=openai

# OpenAI
OPENAI_API_KEY=sk-xxx
# OPENAI_API_BASE_URL=https://api.openai.com/v1
# OPENAI_DEFAULT_MODEL=gpt-5.1-mini
# OPENAI_TIMEOUT_SECONDS=45
# OPENAI_FALLBACK_MODEL=gpt-5-mini

# Perplexity (opcional)
# PERPLEXITY_API_KEY=
# PERPLEXITY_DEFAULT_MODEL=sonar-pro
# PERPLEXITY_API_BASE_URL=https://api.perplexity.ai/chat/completions
# PERPLEXITY_TIMEOUT_SECONDS=90
# PERPLEXITY_MAX_CONTINUATIONS=2

# Gemini (opcional)
# GEMINI_API_KEY=
# GEMINI_DEFAULT_MODEL=gemini-1.5-pro

# ── Citaciones ────────────────────────────────────────────────────────
# CITATION_CORPUS_VERSION=v1
# CITATION_CACHE_TTL_MINUTES=720
